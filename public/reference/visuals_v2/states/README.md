# states visuals

This folder contains correctly named AlphaVest WealthOS v2 visual references. Only the actual app/screen/drawer/modal/matrix area should be implemented as UI. Annotations, legends, dev notes and workflow explanations must be interpreted as implementation logic, docs and tests, not copied verbatim into the client UI.
